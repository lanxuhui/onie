# Accton cx732q_n ONIE installer configuration

description="Asterfusion, cx732q_n"

mass_dev="storage-not-found"
discover_mass_storage()
{
    mass_bus="$1"
    for i in a b c d e ; do
        if $(ls -l /sys/block/sd$i/device 2>/dev/null | grep -q "$mass_bus") ; then
            mass_dev="/dev/sd$i"
            return 0
        fi
    done
    return 1
}

# Default ONIE block device
# 0:0:0 means d1508 or d1527
# 4:0:0 means c3000
install_device_platform()
{
    discover_mass_storage "target4:0:0" ||
    discover_mass_storage "1-1.4:1.0"   ||
    discover_mass_storage "target0:0:0" ||
    discover_mass_storage "0000:00:1f.2"
    ret=$?
    echo "$mass_dev"
    return $ret
}

# Local Variables:
# mode: shell-script
# eval: (sh-set-shell "/bin/sh" t nil)
# End:
